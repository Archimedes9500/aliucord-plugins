version = "1.0.0"
description = "Lets you remove Discord UI animations without disabling emoji, gif and sticker autoplay (your settings still apply)."

aliucord {
	changelog.set(
		"""
		""".trimIndent()
	)
}
