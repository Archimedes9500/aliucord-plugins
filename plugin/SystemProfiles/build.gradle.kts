version = "1.0.0"
description = "Allows you to see profiles of system message (welcome, poll results, etc.) authors by clicking it's icon."

aliucord {
	changelog.set(
		"""
			**1.0.0** Actually works
		""".trimIndent()
	)
}
