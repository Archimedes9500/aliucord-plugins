version = "0.0"
description = "Stores discord settings in a plugin settings file (`Discord.json`) that can be easily transferred between Aliucord instances"

aliucord {
	changelog.set(
		"""
		""".trimIndent()
	)
}
