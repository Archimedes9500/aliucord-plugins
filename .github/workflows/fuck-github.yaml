name: Run
on: 
  workflow_dispatch:
    inputs:
      params:
        required: false
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v5
      - name: run
        run: |
         run() {
          	local command=""
          	local arg=""
          	for arg in "$@"; do
           		if [[ $arg != "--" ]]; then
           			command+="$arg "
          		else
          			eval "$command"
          			command=""
          		fi
          	done
          	eval "$command"
         }
         run ${{ github.event.inputs.params }}	
